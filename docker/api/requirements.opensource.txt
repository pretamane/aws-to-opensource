# Python Requirements for Open-Source Stack
# Replaces AWS-specific dependencies

# Core FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database - PostgreSQL (replaces DynamoDB)
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
asyncpg==0.29.0

# Search - Meilisearch (replaces OpenSearch)
meilisearch==0.31.0

# Object Storage - MinIO with S3 API (replaces AWS S3/EFS)
boto3==1.34.0          # S3-compatible API for MinIO
minio==7.2.0           # Native MinIO client (alternative)

# Email - AWS SES (kept for cost efficiency)
botocore==1.34.0

# Data validation and models
pydantic[email]==2.5.0

# File handling and async operations
aiofiles==23.2.1

# HTTP client
requests==2.31.0
httpx==0.25.2

# Utilities
python-dateutil==2.8.2
typing-extensions==4.8.0

# Monitoring - Prometheus metrics
prometheus-client==0.19.0

# Logging - Structured logging
python-json-logger==2.0.7

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1


